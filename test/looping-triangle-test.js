import test from 'ava';
import loopingTriangle from '../src/js/modules/02-program-structure/looping-triangle';

test.todo('expect a triangle of hashes to be returned', t => {
    // Need to stub console.log and then check number of times
    // it's called and what it's outputting.
    // t.is(loopingTriangle.init(), '#######');
});